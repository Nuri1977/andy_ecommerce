<% if obj.errors.any? %>
  <div class="notification is-warning">
    <button class="delete"></button>
    <h2 class=" title is-5 pt2"><%= pluralize(obj.errors.count, "error") %> prohibited this record from being saved:</h2>

    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>